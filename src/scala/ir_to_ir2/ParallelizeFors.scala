package compile

object ParallelizeFors {
  import IR._
  import IRShared._

  def parallelize(program:ProgramIR) : ProgramIR = {
    // match on for loops
  
    // is loop parallelizable?
  
    // parallelize the loop
  
  }

  def parallelizeLoop(loop:For) : ??? = {

  }

  def isLoopParallelizable(loop:For) : Boolean = {

  }


}
